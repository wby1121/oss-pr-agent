const translations = {
  en: {
    "toolbar.language": "Language",
    "toolbar.theme": "Theme",
    "theme.light": "Day",
    "theme.dark": "Night",
    "hero.eyebrow": "Open source contribution studio",
    "hero.title": "Plan PRs from real project discussions, one confirmation step at a time.",
    "hero.lede": "Review repository signals, select a bug or feature request, shape the solution, edit Markdown drafts, and prepare a submission branch without leaving the page.",
    "flow.title": "Flow",
    "flow.step1": "Analyze target repo",
    "flow.step2": "Confirm solution",
    "flow.step3": "Edit PR and reply",
    "flow.step4": "Prepare branch",
    "steps.one": "Step 1",
    "steps.two": "Step 2",
    "steps.three": "Step 3",
    "steps.four": "Step 4",
    "panel1.title": "Input target address",
    "panel1.label": "GitHub repository URL",
    "panel1.button": "Analyze",
    "panel1.hint": "This returns the project star count plus the most relevant bug reports and feature requests.",
    "panel2.title": "Confirm solution",
    "panel2.empty": "Analyze a repository first.",
    "panel2.label": "Editable solution outline",
    "panel2.button": "Confirm this solution",
    "panel3.title": "PR and comment reply",
    "panel3.prTitle": "PR title",
    "panel3.prBody": "PR body (Markdown)",
    "panel3.replyBody": "Reply draft (Markdown)",
    "panel3.button": "Confirm and preview drafts",
    "panel3.previewPr": "PR preview",
    "panel3.previewReply": "Reply preview",
    "panel4.title": "Submit branch and wait for confirmation",
    "panel4.empty": "Confirm the drafts before preparing a branch.",
    "panel4.button": "Prepare branch",
    "dynamic.openIssue": "Open issue",
    "dynamic.openRepo": "Open repository",
    "dynamic.openDiscussion": "Open issue discussion",
    "dynamic.noDescription": "No description provided.",
    "dynamic.unknownLanguage": "unknown language",
    "dynamic.score": "score",
    "dynamic.stars": "stars",
    "dynamic.commentSignals": "comment signals",
    "dynamic.noMajorTitle": "No major bug or request found",
    "dynamic.noMajorBody": "The repository passed inspection, but no qualifying issue discussion was available yet.",
    "dynamic.noRecommendationTitle": "No recommendation yet",
    "dynamic.noRecommendationBody": "No suitable issue discussion was found to propose a solution.",
    "dynamic.solutionOutline": "Solution outline",
    "dynamic.draftsSavedTitle": "Drafts saved",
    "dynamic.draftsSavedBody": "The Markdown drafts are confirmed and ready for branch preparation.",
    "dynamic.sessionLog": "Session log",
    "dynamic.branchPreparedTitle": "Branch prepared",
    "dynamic.branchPreparedBody": "is queued and waiting for your final confirmation.",
    "dynamic.submissionFile": "Submission file",
    "dynamic.kindBug": "bug",
    "dynamic.kindFeature": "feature",
    "dynamic.kindGeneral": "general"
  },
  zh: {
    "toolbar.language": "语言",
    "toolbar.theme": "主题",
    "theme.light": "日间",
    "theme.dark": "夜间",
    "hero.eyebrow": "开源协作工作台",
    "hero.title": "基于真实项目讨论规划 PR，并且一步一步确认后再继续。",
    "hero.lede": "查看仓库信号，挑选 bug 或需求，确认解决方案，编辑 Markdown 格式的 PR 与评论回复，再准备待确认分支。",
    "flow.title": "流程",
    "flow.step1": "分析目标仓库",
    "flow.step2": "确认解决方案",
    "flow.step3": "编辑 PR 与回复",
    "flow.step4": "准备分支",
    "steps.one": "步骤 1",
    "steps.two": "步骤 2",
    "steps.three": "步骤 3",
    "steps.four": "步骤 4",
    "panel1.title": "输入目标地址",
    "panel1.label": "GitHub 仓库地址",
    "panel1.button": "开始分析",
    "panel1.hint": "返回项目 star 数，以及最主要的 bug 和需求讨论。",
    "panel2.title": "确认解决方案",
    "panel2.empty": "请先分析一个仓库。",
    "panel2.label": "可编辑解决方案大纲",
    "panel2.button": "确认采用此方案",
    "panel3.title": "PR 与评论回复",
    "panel3.prTitle": "PR 标题",
    "panel3.prBody": "PR 正文（Markdown）",
    "panel3.replyBody": "回复草稿（Markdown）",
    "panel3.button": "确认并预览草稿",
    "panel3.previewPr": "PR 预览",
    "panel3.previewReply": "回复预览",
    "panel4.title": "提交分支并等待确认",
    "panel4.empty": "请先确认草稿，再准备分支。",
    "panel4.button": "准备分支",
    "dynamic.openIssue": "打开 Issue",
    "dynamic.openRepo": "打开仓库",
    "dynamic.openDiscussion": "打开讨论",
    "dynamic.noDescription": "暂无描述。",
    "dynamic.unknownLanguage": "未知语言",
    "dynamic.score": "评分",
    "dynamic.stars": "stars",
    "dynamic.commentSignals": "条评论信号",
    "dynamic.noMajorTitle": "暂未发现主要 bug 或需求",
    "dynamic.noMajorBody": "仓库本身通过了检查，但暂时没有拿到符合条件的 issue 讨论。",
    "dynamic.noRecommendationTitle": "暂未生成推荐方案",
    "dynamic.noRecommendationBody": "目前没有足够合适的 issue 讨论来生成方案。",
    "dynamic.solutionOutline": "方案大纲",
    "dynamic.draftsSavedTitle": "草稿已保存",
    "dynamic.draftsSavedBody": "Markdown 草稿已经确认，可以继续准备分支。",
    "dynamic.sessionLog": "会话日志",
    "dynamic.branchPreparedTitle": "分支已准备",
    "dynamic.branchPreparedBody": "已进入等待你最终确认的状态。",
    "dynamic.submissionFile": "提交文件",
    "dynamic.kindBug": "bug",
    "dynamic.kindFeature": "需求",
    "dynamic.kindGeneral": "通用"
  },
  ja: {
    "toolbar.language": "言語",
    "toolbar.theme": "テーマ",
    "theme.light": "昼",
    "theme.dark": "夜",
    "hero.eyebrow": "オープンソース貢献スタジオ",
    "hero.title": "実際の議論をもとに PR を計画し、各段階で確認して進めます。",
    "hero.lede": "リポジトリのシグナルを確認し、バグや要望を選び、解決方針を整え、Markdown の PR と返信を編集し、確認待ちのブランチを準備します。",
    "flow.title": "フロー",
    "flow.step1": "対象リポジトリを分析",
    "flow.step2": "解決方針を確認",
    "flow.step3": "PR と返信を編集",
    "flow.step4": "ブランチを準備",
    "steps.one": "ステップ 1",
    "steps.two": "ステップ 2",
    "steps.three": "ステップ 3",
    "steps.four": "ステップ 4",
    "panel1.title": "対象 URL を入力",
    "panel1.label": "GitHub リポジトリ URL",
    "panel1.button": "分析する",
    "panel1.hint": "プロジェクトの star 数と主要なバグ・要望を返します。",
    "panel2.title": "解決方針を確認",
    "panel2.empty": "先にリポジトリを分析してください。",
    "panel2.label": "編集可能な解決方針アウトライン",
    "panel2.button": "この方針で確定",
    "panel3.title": "PR とコメント返信",
    "panel3.prTitle": "PR タイトル",
    "panel3.prBody": "PR 本文（Markdown）",
    "panel3.replyBody": "返信ドラフト（Markdown）",
    "panel3.button": "ドラフトを確認してプレビュー",
    "panel3.previewPr": "PR プレビュー",
    "panel3.previewReply": "返信プレビュー",
    "panel4.title": "ブランチを準備して確認待ちへ",
    "panel4.empty": "先にドラフトを確定してください。",
    "panel4.button": "ブランチを準備",
    "dynamic.openIssue": "Issue を開く",
    "dynamic.openRepo": "リポジトリを開く",
    "dynamic.openDiscussion": "議論を開く",
    "dynamic.noDescription": "説明はありません。",
    "dynamic.unknownLanguage": "不明な言語",
    "dynamic.score": "スコア",
    "dynamic.stars": "stars",
    "dynamic.commentSignals": "件のコメントシグナル",
    "dynamic.noMajorTitle": "主要なバグや要望は未検出",
    "dynamic.noMajorBody": "リポジトリ自体は条件を満たしましたが、対象になる issue 議論はまだ見つかりませんでした。",
    "dynamic.noRecommendationTitle": "まだ推奨案はありません",
    "dynamic.noRecommendationBody": "方針を提案できる十分な issue 議論が見つかりませんでした。",
    "dynamic.solutionOutline": "方針アウトライン",
    "dynamic.draftsSavedTitle": "ドラフトを保存しました",
    "dynamic.draftsSavedBody": "Markdown ドラフトは確認済みで、ブランチ準備に進めます。",
    "dynamic.sessionLog": "セッションログ",
    "dynamic.branchPreparedTitle": "ブランチを準備しました",
    "dynamic.branchPreparedBody": "最終確認待ちの状態です。",
    "dynamic.submissionFile": "提出ファイル",
    "dynamic.kindBug": "バグ",
    "dynamic.kindFeature": "要望",
    "dynamic.kindGeneral": "一般"
  },
  ko: {
    "toolbar.language": "언어",
    "toolbar.theme": "테마",
    "theme.light": "주간",
    "theme.dark": "야간",
    "hero.eyebrow": "오픈소스 기여 스튜디오",
    "hero.title": "실제 프로젝트 토론을 바탕으로 PR을 계획하고, 단계별로 확인하며 진행합니다.",
    "hero.lede": "저장소 신호를 검토하고, 버그나 기능 요청을 고르고, 해결안을 다듬고, Markdown 형식의 PR 및 댓글 답변을 편집한 뒤, 확인 대기 중인 브랜치를 준비합니다.",
    "flow.title": "흐름",
    "flow.step1": "대상 저장소 분석",
    "flow.step2": "해결안 확인",
    "flow.step3": "PR 및 답변 편집",
    "flow.step4": "브랜치 준비",
    "steps.one": "1단계",
    "steps.two": "2단계",
    "steps.three": "3단계",
    "steps.four": "4단계",
    "panel1.title": "대상 주소 입력",
    "panel1.label": "GitHub 저장소 URL",
    "panel1.button": "분석하기",
    "panel1.hint": "프로젝트 star 수와 주요 버그 및 요구사항을 반환합니다.",
    "panel2.title": "해결안 확인",
    "panel2.empty": "먼저 저장소를 분석해 주세요.",
    "panel2.label": "수정 가능한 해결안 개요",
    "panel2.button": "이 해결안으로 확정",
    "panel3.title": "PR 및 댓글 답변",
    "panel3.prTitle": "PR 제목",
    "panel3.prBody": "PR 본문 (Markdown)",
    "panel3.replyBody": "답변 초안 (Markdown)",
    "panel3.button": "초안 확인 및 미리보기",
    "panel3.previewPr": "PR 미리보기",
    "panel3.previewReply": "답변 미리보기",
    "panel4.title": "브랜치를 준비하고 확인 대기",
    "panel4.empty": "먼저 초안을 확정해 주세요.",
    "panel4.button": "브랜치 준비",
    "dynamic.openIssue": "이슈 열기",
    "dynamic.openRepo": "저장소 열기",
    "dynamic.openDiscussion": "토론 열기",
    "dynamic.noDescription": "설명이 없습니다.",
    "dynamic.unknownLanguage": "알 수 없는 언어",
    "dynamic.score": "점수",
    "dynamic.stars": "stars",
    "dynamic.commentSignals": "개의 댓글 신호",
    "dynamic.noMajorTitle": "주요 버그 또는 요구사항이 없습니다",
    "dynamic.noMajorBody": "저장소 자체는 조건을 통과했지만, 적합한 이슈 토론은 아직 찾지 못했습니다.",
    "dynamic.noRecommendationTitle": "아직 추천안이 없습니다",
    "dynamic.noRecommendationBody": "해결안을 제안할 만큼 적합한 이슈 토론을 찾지 못했습니다.",
    "dynamic.solutionOutline": "해결안 개요",
    "dynamic.draftsSavedTitle": "초안 저장 완료",
    "dynamic.draftsSavedBody": "Markdown 초안이 확정되어 브랜치 준비 단계로 진행할 수 있습니다.",
    "dynamic.sessionLog": "세션 로그",
    "dynamic.branchPreparedTitle": "브랜치 준비 완료",
    "dynamic.branchPreparedBody": "최종 확인을 기다리는 상태입니다.",
    "dynamic.submissionFile": "제출 파일",
    "dynamic.kindBug": "버그",
    "dynamic.kindFeature": "요구사항",
    "dynamic.kindGeneral": "일반"
  }
};

const state = {
  session: null,
  language: localStorage.getItem("oss-pr-agent-language") || "en",
  theme: localStorage.getItem("oss-pr-agent-theme") || "light"
};

const analysisOutput = document.getElementById("analysis-output");
const solutionOutput = document.getElementById("solution-output");
const submissionOutput = document.getElementById("submission-output");
const prPreview = document.getElementById("pr-preview");
const replyPreview = document.getElementById("reply-preview");
const repoUrlInput = document.getElementById("repo-url");
const solutionOutlineInput = document.getElementById("solution-outline");

document.getElementById("analyze-btn").addEventListener("click", analyzeRepo);
document.getElementById("confirm-solution-btn").addEventListener("click", confirmSolution);
document.getElementById("save-drafts-btn").addEventListener("click", saveDrafts);
document.getElementById("submit-branch-btn").addEventListener("click", submitBranch);
document.querySelectorAll("[data-lang]").forEach((button) => {
  button.addEventListener("click", () => setLanguage(button.dataset.lang));
});
document.querySelectorAll("[data-theme]").forEach((button) => {
  button.addEventListener("click", () => setTheme(button.dataset.theme));
});

setLanguage(state.language);
setTheme(state.theme);

async function analyzeRepo() {
  const repoUrl = repoUrlInput.value.trim();
  if (!repoUrl) return;
  const response = await postJson("/api/analyze", { repo_url: repoUrl });
  state.session = response.session;
  renderAnalysis();
  renderSolution();
  unlockStep("step-2");
}

async function confirmSolution() {
  const solution = state.session?.recommended;
  if (!solution) return;
  const outline = solutionOutlineInput.value
    .split("\n")
    .map((item) => item.replace(/^- /, "").trim())
    .filter(Boolean);
  const response = await postJson("/api/confirm-solution", {
    session_id: state.session.id,
    priority_reason: solution.priority_reason,
    implementation_outline: outline.length ? outline : solution.implementation_outline,
  });
  state.session = response.session;
  fillDrafts();
  unlockStep("step-3");
}

async function saveDrafts() {
  const response = await postJson("/api/update-drafts", {
    session_id: state.session.id,
    pr_title: document.getElementById("pr-title").value,
    pr_body: document.getElementById("pr-body").value,
    reply_body: document.getElementById("reply-body").value,
  });
  state.session = response.session;
  prPreview.innerHTML = response.preview.pr_body_html;
  replyPreview.innerHTML = response.preview.reply_body_html;
  submissionOutput.innerHTML = `<div class="data-card"><h3>${t("dynamic.draftsSavedTitle")}</h3><p>${t("dynamic.draftsSavedBody")}</p><p>${t("dynamic.sessionLog")}: <code>${state.session.log_path}</code></p></div>`;
  document.getElementById("submit-branch-btn").disabled = false;
  unlockStep("step-4");
}

async function submitBranch() {
  const response = await postJson("/api/submit-branch", {
    session_id: state.session.id,
  });
  state.session = response.session;
  const submission = state.session.submission;
  submissionOutput.innerHTML = `
    <div class="data-card">
      <h3>${t("dynamic.branchPreparedTitle")}</h3>
      <p><strong>${submission.branch_name}</strong> ${t("dynamic.branchPreparedBody")}</p>
      <p>${t("dynamic.submissionFile")}: <code>${submission.path}</code></p>
      <p>${t("dynamic.sessionLog")}: <code>${state.session.log_path}</code></p>
    </div>
  `;
}

function renderAnalysis() {
  const { repo, candidates, scorecard } = state.session;
  analysisOutput.classList.remove("hidden");

  const candidateCards = candidates.length
    ? candidates.slice(0, 4).map((candidate) => `
      <div class="data-card">
        <h3>${escapeHtml(candidate.title)}</h3>
        <p>${escapeHtml(candidate.priority_reason)}</p>
        <div class="chip-row">
          <span class="chip">${kindLabel(candidate.kind)}</span>
          <span class="chip">#${candidate.issue_number}</span>
          <span class="chip">${candidate.comments_count} ${t("dynamic.commentSignals")}</span>
        </div>
        <p><a href="${candidate.url}" target="_blank" rel="noreferrer">${t("dynamic.openIssue")}</a></p>
      </div>
    `).join("")
    : `<div class="data-card"><h3>${t("dynamic.noMajorTitle")}</h3><p>${t("dynamic.noMajorBody")}</p></div>`;

  analysisOutput.innerHTML = `
    <div class="data-card">
      <h3>${escapeHtml(repo.full_name)}</h3>
      <p>${escapeHtml(repo.description || t("dynamic.noDescription"))}</p>
      <div class="chip-row">
        <span class="chip">${repo.stargazers_count} ${t("dynamic.stars")}</span>
        <span class="chip">${t("dynamic.score")} ${scorecard.score}</span>
        <span class="chip">${repo.language || t("dynamic.unknownLanguage")}</span>
      </div>
      <p><a href="${repo.html_url}" target="_blank" rel="noreferrer">${t("dynamic.openRepo")}</a></p>
    </div>
    <div class="stack">${candidateCards}</div>
  `;
}

function renderSolution() {
  const recommended = state.session?.recommended;
  if (!recommended) {
    solutionOutput.innerHTML = `<div class="data-card"><h3>${t("dynamic.noRecommendationTitle")}</h3><p>${t("dynamic.noRecommendationBody")}</p></div>`;
    document.getElementById("confirm-solution-btn").disabled = true;
    solutionOutlineInput.value = "";
    return;
  }
  solutionOutput.innerHTML = `
    <div class="data-card">
      <h3>${escapeHtml(recommended.issue_title)}</h3>
      <p>${escapeHtml(recommended.priority_reason)}</p>
      <div class="chip-row">
        <span class="chip">${kindLabel(recommended.issue_kind)}</span>
        <span class="chip">#${recommended.issue_number}</span>
      </div>
      <p><a href="${recommended.issue_url}" target="_blank" rel="noreferrer">${t("dynamic.openDiscussion")}</a></p>
      <h4>${t("dynamic.solutionOutline")}</h4>
      <ul>${recommended.implementation_outline.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>
    </div>
  `;
  solutionOutlineInput.value = recommended.implementation_outline.map((item) => `- ${item}`).join("\n");
  document.getElementById("confirm-solution-btn").disabled = false;
}

function fillDrafts() {
  const { drafts } = state.session;
  document.getElementById("pr-title").value = drafts.pr_title || "";
  document.getElementById("pr-body").value = drafts.pr_body || "";
  document.getElementById("reply-body").value = drafts.reply_body || "";
  document.getElementById("save-drafts-btn").disabled = false;
  prPreview.innerHTML = "";
  replyPreview.innerHTML = "";
}

function unlockStep(id) {
  document.getElementById(id).classList.add("active");
}

function setLanguage(language) {
  state.language = translations[language] ? language : "en";
  localStorage.setItem("oss-pr-agent-language", state.language);
  document.documentElement.lang = state.language;
  document.querySelectorAll("[data-i18n]").forEach((element) => {
    element.textContent = t(element.dataset.i18n);
  });
  repoUrlInput.placeholder = "https://github.com/owner/repo";
  solutionOutlineInput.placeholder = `- ${t("dynamic.kindBug")} reproduction\n- minimal fix\n- tests`;
  syncToggleState("[data-lang]", "lang", state.language);
  if (!state.session) {
    applyEmptyStates();
    return;
  }
  renderAnalysis();
  renderSolution();
  if (state.session.drafts?.pr_body) {
    submissionOutput.innerHTML = `<div class="data-card"><h3>${t("dynamic.draftsSavedTitle")}</h3><p>${t("dynamic.draftsSavedBody")}</p><p>${t("dynamic.sessionLog")}: <code>${state.session.log_path}</code></p></div>`;
  }
  if (state.session.submission) {
    const submission = state.session.submission;
    submissionOutput.innerHTML = `
      <div class="data-card">
        <h3>${t("dynamic.branchPreparedTitle")}</h3>
        <p><strong>${submission.branch_name}</strong> ${t("dynamic.branchPreparedBody")}</p>
        <p>${t("dynamic.submissionFile")}: <code>${submission.path}</code></p>
        <p>${t("dynamic.sessionLog")}: <code>${state.session.log_path}</code></p>
      </div>
    `;
  }
}

function setTheme(theme) {
  state.theme = theme === "dark" ? "dark" : "light";
  localStorage.setItem("oss-pr-agent-theme", state.theme);
  document.body.dataset.theme = state.theme;
  syncToggleState("[data-theme]", "theme", state.theme);
}

function syncToggleState(selector, key, value) {
  document.querySelectorAll(selector).forEach((button) => {
    button.classList.toggle("is-active", button.dataset[key] === value);
  });
}

function applyEmptyStates() {
  solutionOutput.textContent = t(solutionOutput.dataset.emptyKey);
  submissionOutput.textContent = t(submissionOutput.dataset.emptyKey);
}

function kindLabel(kind) {
  if (kind === "bug") return t("dynamic.kindBug");
  if (kind === "feature") return t("dynamic.kindFeature");
  return t("dynamic.kindGeneral");
}

function t(key) {
  return translations[state.language]?.[key] || translations.en[key] || key;
}

async function postJson(url, payload) {
  const response = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify(payload),
  });
  const data = await response.json();
  if (!response.ok) {
    throw new Error(data.error || "Request failed");
  }
  return data;
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#39;");
}
